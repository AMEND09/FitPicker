import WeatherWardrobe from './artifacts/default';  // Remove file extension

export default function App() {
  return (
    <main className="min-h-screen bg-background">
      <WeatherWardrobe />
    </main>
  );
}
